{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "definitions": {
        "Record<string,Keys>": {
            "additionalProperties": false,
            "title": "Record<string,Keys>",
            "type": "object"
        }
    },
    "properties": {
        "folderPattern": {
            "title": "folderPattern",
            "type": "string"
        },
        "keys": {
            "$ref": "#/definitions/Record<string,Keys>",
            "title": "keys"
        },
        "pattern": {
            "title": "pattern",
            "type": "string"
        }
    },
    "required": [
        "keys",
        "pattern"
    ],
    "type": "object"
}

